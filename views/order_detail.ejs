<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Chi tiết đơn hàng - MitFruits</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- HEADER HALONA STYLE -->
    <div class="topbar">
        <div class="container">
            <div class="topbar-left">Chuyên cung cấp thực phẩm sạch | MitFruits</div>
            <div class="topbar-right">
                <a href="/login"><span class="topbar-link">Tài khoản</span></a>
                <a href="/cart" class="cart-icon-link"><span class="cart-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#388e3c" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61l1.38-7.39H6"/></svg></span></a>
                <span class="topbar-hotline">Hotline: <b>0123 456 789</b></span>
            </div>
        </div>
    </div>
    <header class="main-header main-header-hl">
        <div class="container">
            <div class="logo"><img src="/images.jpg" alt="Logo" height="44"><span class="brand">MitFruits</span></div>
            <form class="search-bar search-bar-hl"><input type="text" placeholder="Tìm kiếm..."><button type="submit"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></button></form>
            <nav class="nav-hl">
                <a href="/">Trang chủ</a>
                <a href="/shop">Sản phẩm</a>
                <a href="/about">Giới thiệu</a>
                <a href="/news">Tin tức</a>
                <a href="/contact">Liên hệ</a>
            </nav>
        </div>
    </header>
    <!-- END HEADER -->
    <section class="order-detail-section">
        <div class="order-detail-container">
            <h1>Chi tiết đơn hàng</h1>
            <table class="order-detail-table">
                <tr>
                    <th>SẢN PHẨM</th>
                    <th>TỔNG</th>
                </tr>
                <% order.items.forEach(item => { %>
                <tr>
                    <td><a href="/product/<%= item.id %>"><%= item.name %></a> × <%= item.qty %></td>
                    <td><%= (item.price * item.qty).toLocaleString() %>đ</td>
                </tr>
                <% }) %>
                <tr>
                    <td>Tổng số phụ:</td>
                    <td><%= order.total.toLocaleString() %>đ</td>
                </tr>
                <tr>
                    <td>Phương thức thanh toán:</td>
                    <td><%= order.method %></td>
                </tr>
                <tr>
                    <td><b>Tổng cộng:</b></td>
                    <td><b><%= order.total.toLocaleString() %>đ</b></td>
                </tr>
            </table>
            <div class="order-detail-thank">
                <div class="order-detail-thank-box">
                    <b style="color:#388e3c">Cảm ơn bạn. Đơn hàng của bạn đã được nhận.</b>
                    <ul>
                        <li>Mã đơn hàng: <b><%= order.code %></b></li>
                        <li>Ngày: <b><%= order.date %></b></li>
                        <li>Tổng cộng: <b><%= order.total.toLocaleString() %>đ</b></li>
                        <li>Phương thức thanh toán: <b><%= order.method %></b></li>
                    </ul>
                </div>
            </div>
        </div>
    </section>
    <footer>
        <div class="container">
            <div class="footer-info">
                <b>MitFruits</b> - Địa chỉ: 123 Đường Mít, Hà Nội | Hotline: 0123 456 789
            </div>
            <div class="footer-copy">&copy; 2024 MitFruits. All rights reserved.</div>
        </div>
    </footer>
</body>
</html> 